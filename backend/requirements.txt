fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# Authentication
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0

# Database & Enterprise Drivers
# SQLite is built-in; PostgreSQL driver for enterprise deployment
psycopg2-binary>=2.9.9

# PDF text extraction
pypdf>=4.2.0

# File upload
python-multipart>=0.0.9

# HTTP client (Ollama calls)
httpx>=0.27.0

# OCR & image processing
# pytesseract and opencv are optional — graceful fallback if not installed
# pytesseract>=0.3.10
# opencv-python-headless>=4.9.0
# paddleocr>=2.7.0
Pillow>=10.3.0

# Vector DB & Semantic Retrieval
# sentence-transformers>=2.7.0
# chromadb>=0.5.0

# LangChain Agentic Routing
# langchain>=0.2.0
# langchain-community>=0.2.0

# Utilities
python-dateutil>=2.9.0
